(function (global, factory) {
  if (typeof define === "function" && define.amd) {
    define(["exports", "./cadesplugin_api"], factory);
  } else if (typeof exports !== "undefined") {
    factory(exports, require("./cadesplugin_api"));
  } else {
    var mod = {
      exports: {}
    };
    factory(mod.exports, global.cadesplugin_api);
    global.index = mod.exports;
  }
})(this, function (exports) {
  "use strict";

  Object.defineProperty(exports, "__esModule", {
    value: true
  });
  exports.CadesPluginer = undefined;

  function _classCallCheck(instance, Constructor) {
    if (!(instance instanceof Constructor)) {
      throw new TypeError("Cannot call a class as a function");
    }
  }

  var _createClass = function () {
    function defineProperties(target, props) {
      for (var i = 0; i < props.length; i++) {
        var descriptor = props[i];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }

    return function (Constructor, protoProps, staticProps) {
      if (protoProps) defineProperties(Constructor.prototype, protoProps);
      if (staticProps) defineProperties(Constructor, staticProps);
      return Constructor;
    };
  }();

  var _class, _temp;

  var CadesPluginer = (_temp = _class = function () {
    function CadesPluginer() {
      _classCallCheck(this, CadesPluginer);
    }

    _createClass(CadesPluginer, null, [{
      key: "createObject",
      value: function createObject(name) {
        return !!window.cadesplugin.CreateObjectAsync ? window.cadesplugin.CreateObjectAsync(name) : new Promise(function (resolve, reject) {
          var obj = window.cadesplugin.CreateObject(name);
          resolve(obj);
        });
      }
    }, {
      key: "openStore",
      value: function openStore(store) {
        var _this = this;

        for (var _len = arguments.length, params = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
          params[_key - 1] = arguments[_key];
        }

        return new Promise(function (resolve, reject) {
          if (!window.cadesplugin.CreateObjectAsync) {
            store.Open.apply(_this, params);
            resolve(store);
          } else {
            store.Open.apply(_this, params).then(function () {
              return resolve(store);
            });
          }
        });
      }
    }, {
      key: "getCertsObjFromOpenStore",
      value: function getCertsObjFromOpenStore(store) {
        return new Promise(function (resolve, reject) {
          if (!!window.cadesplugin.CreateObjectAsync) {
            return store.Certificates.then(function (obj) {
              return obj.Find(CadesPluginer.CAPICOM_CERTIFICATE_FIND_SUBJECT_NAME, "");
            }).then(function (certs) {
              resolve(certs);
            });
          } else {
            return resolve(store.Certificates.Find(CadesPluginer.CAPICOM_CERTIFICATE_FIND_SUBJECT_NAME, ""));
          }
        });
      }
    }, {
      key: "getCountFromCerts",
      value: function getCountFromCerts(certs) {
        return new Promise(function (resolve, reject) {
          if (!!window.cadesplugin.CreateObjectAsync) {
            certs.Count.then(function (count) {
              return resolve(count);
            });
          } else {
            resolve(certs.Count);
          }
        });
      }
    }, {
      key: "getCompanyNameFromSubject",
      value: function getCompanyNameFromSubject(subj) {
        return CadesPluginer.getByTagFromSubject(subj, "O");
      }
    }, {
      key: "getNameFromSubject",
      value: function getNameFromSubject(subj) {
        var name = "";

        name = CadesPluginer.getByTagFromSubject(subj, "SN") + " ";
        name += CadesPluginer.getByTagFromSubject(subj, "G");
        name = name.trim();

        if (!name.length) {
          name = CadesPluginer.getByTagFromSubject(subj, "CN");
        }

        return name;
      }
    }, {
      key: "getByTagFromSubject",
      value: function getByTagFromSubject(subj, tag) {
        var splitted = subj.split(",");
        var val = "";
        splitted.forEach(function (sp) {
          var spl = sp.split("=");
          if (spl[0].trim() === tag) val = spl[1].trim();
        });
        return val;
      }
    }, {
      key: "getCertsByCount",
      value: function getCertsByCount(certs, count) {
        return new Promise(function (resolve, reject) {
          if (!!window.cadesplugin.CreateObjectAsync) {
            var promises = [];
            for (var i = 1; i < count + 1; i++) {
              promises.push(certs.Item(i));
            }
            Promise.all(promises).then(function (certificates) {
              var namePromises = [];
              var fromPromises = [];
              var toPromises = [];
              var serialPromises = [];

              certificates.forEach(function (c) {
                namePromises.push(c.SubjectName);
                fromPromises.push(c.ValidFromDate);
                toPromises.push(c.ValidToDate);
                serialPromises.push(c.SerialNumber);
              });

              Promise.all([namePromises, fromPromises, toPromises, serialPromises].map(function (c) {
                return Promise.all(c);
              })).then(function (names) {
                resolve(certificates.map(function (c, i) {
                  return { certificate: c, info: names.map(function (info) {
                      return info[i];
                    }) };
                }));
              });
            });
          } else {
            // console.log('here parse it certs');
            // console.log('certs', certs);
            // console.log('count', count);
            // const cert = certificatesList.Item(i);
            // signaturesObjList.push(cert);
            //
            // model.signaturesList.push({
            //   company: parseCert(cert.SubjectName, 'O'),
            //   email: parseCert(cert.SubjectName, 'E'),
            //   fromDate: new Date(cert.ValidFromDate),
            //   toDate: new Date(cert.ValidToDate),
            //   name: getNameFromCertificate(cert.SubjectName)
            // });
          }
        });
      }
    }, {
      key: "getAsyncCertArray",
      value: function getAsyncCertArray() {
        return new Promise(function (resolve, reject) {
          CadesPluginer.init().then(function () {
            return CadesPluginer.createObject(CadesPluginer.STORE_OBJECT_NAME);
          }).then(function (store) {
            return CadesPluginer.openStore(store, CadesPluginer.CAPICOM_CURRENT_USER_STORE, CadesPluginer.CAPICOM_MY_STORE, CadesPluginer.CAPICOM_STORE_OPEN_MAXIMUM_ALLOWED);
          }).then(function (store) {
            CadesPluginer.getCertsObjFromOpenStore(store).then(function (certs) {
              return CadesPluginer.getCountFromCerts(certs).then(function (count) {
                return CadesPluginer.getCertsByCount(certs, count);
              });
            }).then(function (certificates) {
              store.Close();
              resolve(certificates);
            });
          }).catch(function (err) {
            return reject(err);
          });
        });
      }
    }, {
      key: "getSyncCertArray",
      value: function getSyncCertArray() {

        return new Promise(function (resolve, reject) {
          var oStore = window.cadesplugin.CreateObject("CAdESCOM.Store");
          oStore.Open(CadesPluginer.CAPICOM_CURRENT_USER_STORE, CadesPluginer.CAPICOM_MY_STORE, CadesPluginer.CAPICOM_STORE_OPEN_MAXIMUM_ALLOWED);

          var oCertificates = oStore.Certificates.Find(CadesPluginer.CAPICOM_CERTIFICATE_FIND_SUBJECT_NAME, "");

          if (oCertificates.Count === 0) {
            alert("Certificate not found");
            return resolve([]);
          }

          var certs = [];

          for (var i = 1; i <= oCertificates.Count; i++) {
            var cert = oCertificates.Item(i);

            certs.push({
              certificate: cert,
              info: [cert.SubjectName, cert.ValidFromDate, cert.ValidToDate, cert.SerialNumber]
            });
          }
          //oStore.Close();
          resolve(certs);
        });
      }
    }, {
      key: "getFinalCertsArray",
      value: function getFinalCertsArray() {
        if (!!window.cadesplugin.CreateObjectAsync) {
          return CadesPluginer.getAsyncCertArray();
        } else {
          return CadesPluginer.getSyncCertArray();
        }
      }
    }, {
      key: "signMessageAsync",
      value: function signMessageAsync(message, cert, detached) {
        return new Promise(function (resolve, reject) {
          window.cadesplugin.CreateObjectAsync("CAdESCOM.CPSigner").then(function (oSigner) {
            oSigner.propset_Certificate(cert).then(function () {
              return window.cadesplugin.CreateObjectAsync("CAdESCOM.CadesSignedData");
            }).then(function (oSignedData) {
              oSignedData.propset_Content(message).then(function () {
                return oSignedData.SignCades(oSigner, CadesPluginer.CADESCOM_CADES_BES, detached);
              }).then(function (msg) {
                resolve(msg);
              });
            });
          });
        });
      }
    }, {
      key: "isActiveX",
      value: function isActiveX() {
        try {
          new window.ActiveXObject("CAdESCOM.store");
          return true;
        } catch (e) {
          return false;
        }
      }
    }, {
      key: "signMessageSync",
      value: function signMessageSync(message, cert, detached) {
        var _this2 = this;

        return new Promise(function (resolve, reject) {
          window.cadesplugin.CADESCOM_ENCODE_BASE64 = 1;
          var CADESCOM_CADES_BES = 1;

          var oSigner = window.cadesplugin.CreateObject("CAdESCOM.CPSigner");
          oSigner.Certificate = cert;
          var oSignedData = window.cadesplugin.CreateObject("CAdESCOM.CadesSignedData");

          if (detached) {
            oSignedData.ContentEncoding = _this2.CADESCOM_BASE64_TO_BINARY;
          }

          oSignedData.Content = message;

          try {
            var signed = oSignedData.SignCades(oSigner, CADESCOM_CADES_BES, detached);
            resolve(signed);
          } catch (e) {
            console.log("error", JSON.stringify(e));
          }
        });
      }
    }, {
      key: "signMessage",
      value: function signMessage(message, cert) {
        var detached = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;

        if (!!window.cadesplugin.CreateObjectAsync) {
          return CadesPluginer.signMessageAsync(message, cert, detached);
        } else {
          return CadesPluginer.signMessageSync(message, cert, detached);
        }
      }
    }, {
      key: "init",
      value: function init() {
        return new Promise(function (resolve, reject) {
          if (!!window.cadesplugin.CreateObjectAsync) {
            window.cadesplugin.then(function () {
              resolve();
            }, function (error) {
              reject(error);
            });
          } else {
            window.addEventListener("message", function (event) {
              if (event.data === "cadesplugin_loaded") {
                resolve();
              } else if (event.data === "cadesplugin_load_error") {
                reject(event);
              }
            }, false);
            window.postMessage("cadesplugin_echo_request", "*");
          }
        });
      }
    }]);

    return CadesPluginer;
  }(), _class.CADESCOM_CADES_BES = 1, _class.CAPICOM_CURRENT_USER_STORE = 2, _class.CAPICOM_MY_STORE = "My", _class.CAPICOM_STORE_OPEN_MAXIMUM_ALLOWED = 2, _class.CAPICOM_CERTIFICATE_FIND_SUBJECT_NAME = 1, _class.STORE_OBJECT_NAME = "CAdESCOM.Store", _class.CADESCOM_CADES_X_LONG_TYPE_1 = 0x5d, _class.CADESCOM_BASE64_TO_BINARY = 1, _temp);
  exports.default = CadesPluginer;
  exports.CadesPluginer = CadesPluginer;
});